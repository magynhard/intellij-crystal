# Tight `*`/`**` after a call name are splat arguments (the compiler only
# rejects them followed by whitespace); spaced forms stay binary operators.
start_attribute *args, **nargs
forward *items
total = base * factor
power = base ** exponent
scaled = value *2
