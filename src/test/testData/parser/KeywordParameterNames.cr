# Keyword identifiers as standalone parameter names (compiler-verified subset)
# (compiler type_declaration_visitor common_descendant... etc.)

def declare_class_var(node, var, uninitialized)
  _then = node
end

def map(values, of = nil, &)
  yield values
end

def parse_c_struct_or_union(union : Bool)
  union
end

def common_descendent_union(type, union)
  [of, union]
end

def defaults(of = nil, forall = 1)
  of = nil
  forall = 1
  of
end

def solefor(forall = 1)
  forall
end

def splatstar(*union)
  union
end

def starred(*union)
  union
end

def block_param(&of)
  of
end
