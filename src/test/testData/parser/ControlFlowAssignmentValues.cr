return @cached_body = "" if content_type.nil?
break selected = candidate if ready?
next offset = offset + 1 unless done?

value = condition || return fallback = compute
value = condition || break selected = candidate
value = condition || next offset = compute
