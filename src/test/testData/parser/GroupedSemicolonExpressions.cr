# Parenthesized groups hold several semicolon-separated expressions, with
# the last one as the value (the compiler's parse_parenthesized_expression;
# primitives.cr ternary branches).
result = checked ? (sign_extend(7, node: node); convert(node: node)) : fallback
pair = (first; second)
setup = (name = build; run(name))
value = (compute)
